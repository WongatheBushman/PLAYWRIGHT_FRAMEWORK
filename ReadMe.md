How to Use:
Open Terminal and run commands to start testing

Commands:
"npm run test" -> runs all tests in the tests-directory
"npm run test:unit" --> runs all tests in the unit-subdirectory of the tests-directory
For full list of commands see the scripts section in package.json
